# بروتوكول الدراسة — حالة كتالوجات المتاجر السعودية 2026
**مجمّد بتاريخ 2026-09-15 قبل جمع أي بيانات. أي تعديل بعد هذا التاريخ يُسجَّل في ذيل الوثيقة بتاريخه وسببه.**

## 1. السؤال البحثي
ما مقدار المعلومات الناقصة في صفحات المنتج بالمتاجر الإلكترونية السعودية على سلة وزد وشوبيفاي؟

الدراسة **وصفية** فقط. لا تدّعي أي علاقة سببية بالمبيعات، ولا تملك بيانات مبيعات.

## 2. إطار العيّنة
لا يوجد سجل عام قابل للتعداد لمتاجر السعودية:
- سجل الشهادات (Certificate Transparency) يعطي 29 مضيفًا على `zid.store` و52 على `salla.sa` فقط،
  لأن المنصتين تستخدمان شهادة شاملة، فلا تظهر المتاجر فرادى. غير صالح.
- منصة «معروف» (maroof.sa) ترد 503 على كل محاولة وصول. غير متاح.

لذلك الإطار **عيّنة مقصودة (purposive)** لا احتمالية، تُبنى من قائمة استعلامات بحث ثابتة تُنشر كاملة
مع الدراسة، منفّذة في نافذة زمنية محددة، مع إزالة التكرار. وهذا هو نفس نوع العيّنة الذي تستخدمه
مؤشرات Baymard («أكبر 344 متجرًا»)، ويجب وصفه بصراحة في النشر ولا يُقدَّم كعيّنة عشوائية.

## 3. معايير الإدراج والاستبعاد
يُدرج المتجر إذا تحققت كل الشروط:
- يعمل ويُرجع HTTP 200.
- منصته سلة أو زد أو شوبيفاي (تُثبَت ببصمة في HTML).
- واجهته بالعربية.
- عملته ريال سعودي.
- له 10 منتجات منشورة على الأقل.
يُستبعد: المتاجر التجريبية، ومتاجر المنصات نفسها، والمتاجر التي تحجب الوصول الآلي،
وأي متجر لم نستطع قراءة 5 صفحات منتج منه.

## 4. وحدة القياس
**صفحة المنتج.** من كل متجر تُسحب 5 صفحات منتج **باختيار عشوائي ببذرة ثابتة (seed=20260915)**
من قائمة منتجاته، لا بأول 5 ولا بالأكثر مبيعًا، حتى لا تنحاز العيّنة لأفضل صفحاته.

## 5. المقاييس — قائمة مجمّدة
لكل صفحة منتج تُسجَّل هذه الحقول. كل حقل إما موجود أو غائب أو تعذّر قياسه (UNMEASURABLE).
الحقل المتعذّر لا يُحسب ضمن الناقص ولا ضمن المكتمل، ويُبلَّغ عن عدده.

| # | الحقل | تعريف إجرائي |
|---|---|---|
| 1 | طول الوصف | عدد كلمات وصف المنتج. يُعدّ ناقصًا إذا كان أقل من 40 كلمة |
| 2 | عدد الصور | صور المنتج نفسه. يُعدّ ناقصًا إذا كانت صورة واحدة أو صفر |
| 3 | الخيارات | وجود خيار واحد على الأقل (حجم، لون، مقاس، تركيز) أو تصريح بعدمها |
| 4 | الفئة | وجود تصنيف أو مسار تنقّل للمنتج |
| 5 | السعر | سعر ظاهر على الصفحة |
| 6 | التوفر | حالة المخزون ظاهرة |
| 7 | التقييمات | وجود تقييم أو عدد مراجعات |
| 8 | العلامة التجارية | حقل ماركة أو مصنّع |
| 9 | الشحن | تكلفة شحن أو مدة توصيل مذكورة في صفحة المنتج |
| 10 | الإرجاع | سياسة إرجاع مذكورة أو مرتبطة من صفحة المنتج |

**المقياس الرئيسي المعلن مسبقًا:** متوسط عدد الحقول الناقصة لكل صفحة منتج، من أصل عشرة.
**المقاييس الثانوية:** نسبة الصفحات التي ينقصها حقل معيّن، والفرق بين المنصات الثلاث.

## 6. طريقة الجمع
- **شوبيفاي:** `/products.json` يعطي البيانات مهيكلة مباشرة.
- **سلة وزد:** خريطة الموقع لاستخراج روابط المنتجات، ثم عرض الصفحة في متصفح بلا واجهة
  لأن المحتوى يُبنى بجافاسكربت.
- تأخير لا يقل عن ثانيتين بين الطلبات لكل مضيف، واحترام `robots.txt`، وحد أقصى 8 طلبات لكل متجر.
- تُحفظ كل صفحة خامًا حتى يمكن إعادة القياس والتدقيق لاحقًا.

## 7. الموثوقية
تُقاس 50 صفحة منتج مرة ثانية بشكل مستقل، وتُحسب نسبة الاتفاق بين القياسين لكل حقل،
وتُنشر النسبة مهما كانت. أي حقل تقلّ فيه نسبة الاتفاق عن 80٪ يُحذف من النتائج المنشورة.

## 8. النشر والأخلاقيات
- صفحات عامة فقط، بلا تسجيل دخول وبلا أي بيانات شخصية.
- لا يُسمّى أي متجر في النشر، ولا تُنشر أي نتيجة على مستوى متجر واحد.
- تُنشر النتائج **مهما كانت**، بما في ذلك لو تبيّن أن الكتالوجات في حالة جيدة.
- تُنشر قائمة الاستعلامات وحجم العيّنة وعدد المستبعدين وأسباب الاستبعاد.
- تُنشر حدود الدراسة صراحة: عيّنة مقصودة، وصفية، بلا بيانات مبيعات.

## 9. ما لا تستطيع هذه الدراسة قوله
- لا شيء عن أثر النقص على المبيعات أو التحويل.
- لا تعميم إحصائي على «كل المتاجر السعودية»، بل على العيّنة المدروسة.

---

## سجل التعديلات بعد التجميد

**2026-09-16 — الحقل 3 (الخيارات): يُنقل من «حقل ناقص» إلى «حقل وصفي».**
السبب: منتج بلا مقاسات أو ألوان ليس منتجًا ناقص البيانات بالضرورة، فكثير من المنتجات
لا تحتمل خيارات أصلًا. احتساب غيابه نقصًا يضخّم مؤشر النقص بلا مبرر.
يبقى الحقل مقيسًا ومنشورًا كنسبة («كم من الصفحات تعرض خيارات») لكنه لا يدخل في
المقياس الرئيسي. صار المقياس الرئيسي: متوسط الحقول الناقصة من أصل **تسعة**.
اكتُشف أثناء اختبار الأداة على 8 متاجر قبل التجميع الكامل، ولم تُحذف أي بيانات.

**2026-09-16 — تصحيحات في أداة القياس، لا في التعريفات.**
ثلاثة كواشف كانت تعطي نتائج خاطئة واكتُشفت بالمراجعة اليدوية قبل التجميع:
السعر (القوالب تعرض الرقم والعملة في عنصرين منفصلين)، وحالة التوفر (صيغة «إضافة للسلة»
لم تكن مغطاة)، والعلامة التجارية (مفتاح brand داخل شفرة القالب ليس حقلًا يراه المشتري).
التعريفات في §5 لم تتغير، والقياس أُعيد على الصفحات المحفوظة نفسها.

**2026-09-16 — نتيجة اختبار الموثوقية: حذف أربعة حقول من النتائج المنشورة.**
رمّز مستقل ثانٍ خمسين صفحة من العيّنة بقراءته، دون اطلاع على مخرجات الأداة ولا على كودها.
نسبة الاتفاق لكل حقل (وقيمة كابا بين قوسين):

| الحقل | الاتفاق | كابا | القرار |
|---|---|---|---|
| السعر | 94٪ | 0.54 | يُنشر |
| التوفر | 92٪ | 0.29 | يُنشر مع تحفظ |
| العلامة التجارية | 91٪ | 0.73 | يُنشر |
| طول الوصف | 85٪ | 0.71 | يُنشر |
| التقييمات | 81٪ | 0.61 | يُنشر |
| الشحن والإرجاع | 79٪ | 0.27 | **يُحذف** |
| الصور | 69٪ | 0.30 | **يُحذف** |
| الخيارات | 60٪ | 0.30 | **يُحذف** |
| التصنيف | 58٪ | 0.15 | **يُحذف** |

الحقول المحذوفة لا تُنشر ولا تدخل المقياس الرئيسي، مهما كانت أرقامها لافتة.
سبب سقوطها تعريفي لا تقني: «صور المنتج» لا تُفصل آليًا عن صور المنتجات المرتبطة في كثير
من القوالب، و«التصنيف» يظهر كمسار بلا معنى («الرئيسية / جميع المنتجات»)، و«الشحن والإرجاع»
يظهر غالبًا في شريط إعلانات عام لا في محتوى المنتج، و«الخيارات» تختلط بالإضافات المدفوعة.

**تحفظ على التوفر والسعر:** اتفاقهما مرتفع لكن قيمة كابا منخفضة لأن الإجابة شبه ثابتة
(الغالبية «موجود»)، فالاتفاق العالي متوقع بالمصادفة. يُنشران كنِسَب وصفية لا كمؤشر تمييز.

صار المقياس الرئيسي: **متوسط الحقول الناقصة من أصل خمسة** (الوصف، السعر، التوفر،
التقييمات، العلامة التجارية).

**2026-09-16 — استبعاد الصفحات التي ليست صفحات منتج.**
كشف المرمّز الثاني أن 4 من 50 صفحة في عيّنة الموثوقية ليست صفحات منتج أصلًا
(صفحة خطأ 410، وصفحتا متجر رئيسيتان، وصفحة قسم فارغة). تُستبعد من التحليل كل صفحة
لا تحمل بيانات منتج مهيكلة ولا تجمع بين سعر ظاهر وحالة توفر.
